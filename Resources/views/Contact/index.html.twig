{% extends "::base.html.twig" %}

{% trans_default_domain 'FruitwareContactBundle' %}

{% block title %}{{ 'fruitware_contact.form_page_title'|trans }}{% endblock %}

{% block content %}
    {{ form_start(form, {'action': path('fruitware_contact_form')}) }}
        <fieldset>
            <legend>{{ 'fruitware_contact.form_title'|trans }}</legend>

            {{ form_widget(form) }}
        </fieldset>
    {{ form_end(form) }}
{% endblock %}
